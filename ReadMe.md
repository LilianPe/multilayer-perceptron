How to use:

first, split the dataset with src/MultilayerPerceptron/splitData.py to create a train set and a validation set

Then create and train a model using src/MultilayerPerceptron/createModel.py